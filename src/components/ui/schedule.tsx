import {Card, CardContent, CardHeader, CardTitle} from "@/components/shadcn-ui/card";

export default function Schedule() {
    return(
        <Card className="card">
            <CardHeader>
                <CardTitle>운동가기</CardTitle>
            </CardHeader>
            <CardContent>
            </CardContent>
        </Card>
    )
}